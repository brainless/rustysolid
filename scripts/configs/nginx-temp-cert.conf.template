server {
    listen 80;
    listen [::]:80;
    server_name {{DOMAIN_NAME}};

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 200 'certbot challenge bootstrap';
        add_header Content-Type text/plain;
    }
}
